{
  "configurations": [
    {
      "name": "Lens config: Launch World`Run",
      "type": "vdm",
      "request": "launch",
      "noDebug": true,
      "defaultName": "World",
      "command": "p new World().Run()"
    }
  ]
}
